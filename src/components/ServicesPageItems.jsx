import React from "react";
import { Container } from ".";

export default function ServicesPageItems({ title, description, image, locale }) {
    return(
        <div>
            <Container>
                my services items 
            </Container>
        </div>
    )
}